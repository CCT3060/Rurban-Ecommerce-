import type { Metadata } from "next";
export const metadata: Metadata = { title: "Join Rurban as a B2B Customer" };
export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
